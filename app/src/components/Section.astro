---
interface Props {
	classOutside?: string
	classInside?: string
}

const { classOutside, classInside } = Astro.props
---

<section class:list={[classOutside]}>
	<div class:list={[classInside, 'container max-w-4xl px-4 py-12 mx-auto']}>
		<slot />
	</div>
</section>
